import {GoogleGenAI} from "@google/genai";\nexport default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});try{const image=req.body?.image;return res.status(400).json({error:"This endpoint expects multipart/form-data. Connect the frontend upload handler to your deployment runtime."})}catch(e){return res.status(500).json({error:"Analysis service error"})}}